#include <algorithm>
#include <unordered_map>
#include <set>
#include <stack>
#include <queue>
#include <unordered_set>
#include <vector>
#include <string>
using namespace std;
int soso(int x)
{
    int res = 0;
    while (x != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int n = x % 10;
        x /= 10;
        res = res * 10 + n;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 10)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 10)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 10)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 10)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res > INT_MAX / 10 || res < INT_MIN / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}

int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
//    两个变量的配合 a 取  挪    b移 插。  口诀。。
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = 10 * res + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = 10 * res + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = res * 10 + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = 10 * res + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = 10 * res + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = 10 * res + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = 10 * res + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = 10 * res + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = 10 * res + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = 10 * res + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = 10 * res + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = 10 * res + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = 10 * res + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = 10 * res + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = 10 * res + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = 10 * res + k;
    }
    return res;
}
int soso(int n)
{
    int res = 0;
    while (n != 0)
    {
        if (res < INT_MIN / 10 || res > INT_MAX / 10)
            return 0;
        int k = n % 10;
        n /= 10;
        res = 10 * res + k;
    }
    return res;
}
// 不管有没有效果 你先跑100圈  不可能没效果的。。
// 而且你没基础 。要靠多刷。。来补基础。。